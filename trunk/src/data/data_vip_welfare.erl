%%%---------------------------------------
%%% @Module  : data_vip_welfare
%%% @Author  : liuzz
%%% @Email   :
%%% @Description: VIP权利配置数据
%%%---------------------------------------


-module(data_vip_welfare).
-export([get/2]).
-include("debug.hrl").


			get(map_jump, 0) -> false
		;

			get(map_jump, 1) -> false
		;

			get(map_jump, 2) -> false
		;

			get(map_jump, 3) -> false
		;

			get(map_jump, 4) -> false
		;

			get(map_jump, 5) -> false
		;

			get(map_jump, 6) -> false
		;

			get(map_jump, 7) -> false
		;

			get(map_jump, 8) -> false
		;

			get(map_jump, 9) -> false
		;

			get(map_jump, 10) -> false
		;

			get(map_jump, 11) -> false
		;

			get(map_jump, 12) -> false
		;

			get(map_jump, 13) -> false
		;

			get(map_jump, 14) -> false
		;

			get(map_jump, 15) -> false
		;

			get(offline_guaji_max_minute, 0) -> 360
		;

			get(offline_guaji_max_minute, 1) -> 360
		;

			get(offline_guaji_max_minute, 2) -> 1440
		;

			get(offline_guaji_max_minute, 3) -> 4320
		;

			get(offline_guaji_max_minute, 4) -> 4320
		;

			get(offline_guaji_max_minute, 5) -> 4320
		;

			get(offline_guaji_max_minute, 6) -> 4320
		;

			get(offline_guaji_max_minute, 7) -> 480
		;

			get(offline_guaji_max_minute, 8) -> 480
		;

			get(offline_guaji_max_minute, 9) -> 480
		;

			get(offline_guaji_max_minute, 10) -> 480
		;

			get(offline_guaji_max_minute, 11) -> 480
		;

			get(offline_guaji_max_minute, 12) -> 720
		;

			get(offline_guaji_max_minute, 13) -> 720
		;

			get(offline_guaji_max_minute, 14) -> 720
		;

			get(offline_guaji_max_minute, 15) -> 720
		;

			get(max_selling_hour, 0) -> 6
		;

			get(max_selling_hour, 1) -> 6
		;

			get(max_selling_hour, 2) -> 6
		;

			get(max_selling_hour, 3) -> 6
		;

			get(max_selling_hour, 4) -> 6
		;

			get(max_selling_hour, 5) -> 6
		;

			get(max_selling_hour, 6) -> 6
		;

			get(max_selling_hour, 7) -> 6
		;

			get(max_selling_hour, 8) -> 6
		;

			get(max_selling_hour, 9) -> 6
		;

			get(max_selling_hour, 10) -> 6
		;

			get(max_selling_hour, 11) -> 6
		;

			get(max_selling_hour, 12) -> 6
		;

			get(max_selling_hour, 13) -> 6
		;

			get(max_selling_hour, 14) -> 6
		;

			get(max_selling_hour, 15) -> 6
		;

			get(dungeon_buy_times, 0) -> 2
		;

			get(dungeon_buy_times, 1) -> 3
		;

			get(dungeon_buy_times, 2) -> 5
		;

			get(dungeon_buy_times, 3) -> 10
		;

			get(dungeon_buy_times, 4) -> 13
		;

			get(dungeon_buy_times, 5) -> 14
		;

			get(dungeon_buy_times, 6) -> 15
		;

			get(dungeon_buy_times, 7) -> 3
		;

			get(dungeon_buy_times, 8) -> 3
		;

			get(dungeon_buy_times, 9) -> 3
		;

			get(dungeon_buy_times, 10) -> 4
		;

			get(dungeon_buy_times, 11) -> 4
		;

			get(dungeon_buy_times, 12) -> 4
		;

			get(dungeon_buy_times, 13) -> 4
		;

			get(dungeon_buy_times, 14) -> 4
		;

			get(dungeon_buy_times, 15) -> 4
		;

			get(answer_acepack_1_times, 0) -> 2
		;

			get(answer_acepack_1_times, 1) -> 2
		;

			get(answer_acepack_1_times, 2) -> 4
		;

			get(answer_acepack_1_times, 3) -> 7
		;

			get(answer_acepack_1_times, 4) -> 7
		;

			get(answer_acepack_1_times, 5) -> 7
		;

			get(answer_acepack_1_times, 6) -> 7
		;

			get(answer_acepack_1_times, 7) -> 8
		;

			get(answer_acepack_1_times, 8) -> 8
		;

			get(answer_acepack_1_times, 9) -> 8
		;

			get(answer_acepack_1_times, 10) -> 8
		;

			get(answer_acepack_1_times, 11) -> 8
		;

			get(answer_acepack_1_times, 12) -> 8
		;

			get(answer_acepack_1_times, 13) -> 8
		;

			get(answer_acepack_1_times, 14) -> 8
		;

			get(answer_acepack_1_times, 15) -> 8
		;

			get(answer_acepack_2_times, 0) -> 2
		;

			get(answer_acepack_2_times, 1) -> 2
		;

			get(answer_acepack_2_times, 2) -> 4
		;

			get(answer_acepack_2_times, 3) -> 7
		;

			get(answer_acepack_2_times, 4) -> 7
		;

			get(answer_acepack_2_times, 5) -> 7
		;

			get(answer_acepack_2_times, 6) -> 7
		;

			get(answer_acepack_2_times, 7) -> 7
		;

			get(answer_acepack_2_times, 8) -> 7
		;

			get(answer_acepack_2_times, 9) -> 7
		;

			get(answer_acepack_2_times, 10) -> 7
		;

			get(answer_acepack_2_times, 11) -> 7
		;

			get(answer_acepack_2_times, 12) -> 7
		;

			get(answer_acepack_2_times, 13) -> 7
		;

			get(answer_acepack_2_times, 14) -> 7
		;

			get(answer_acepack_2_times, 15) -> 7
		;

			get(answer_acepack_3_times, 0) -> 2
		;

			get(answer_acepack_3_times, 1) -> 2
		;

			get(answer_acepack_3_times, 2) -> 4
		;

			get(answer_acepack_3_times, 3) -> 7
		;

			get(answer_acepack_3_times, 4) -> 7
		;

			get(answer_acepack_3_times, 5) -> 7
		;

			get(answer_acepack_3_times, 6) -> 7
		;

			get(answer_acepack_3_times, 7) -> 4
		;

			get(answer_acepack_3_times, 8) -> 4
		;

			get(answer_acepack_3_times, 9) -> 4
		;

			get(answer_acepack_3_times, 10) -> 4
		;

			get(answer_acepack_3_times, 11) -> 4
		;

			get(answer_acepack_3_times, 12) -> 4
		;

			get(answer_acepack_3_times, 13) -> 4
		;

			get(answer_acepack_3_times, 14) -> 4
		;

			get(answer_acepack_3_times, 15) -> 4
		;

			get(arena_extra_buy_times, 0) -> 0
		;

			get(arena_extra_buy_times, 1) -> 5
		;

			get(arena_extra_buy_times, 2) -> 10
		;

			get(arena_extra_buy_times, 3) -> 20
		;

			get(arena_extra_buy_times, 4) -> 20
		;

			get(arena_extra_buy_times, 5) -> 20
		;

			get(arena_extra_buy_times, 6) -> 20
		;

			get(arena_extra_buy_times, 7) -> 10
		;

			get(arena_extra_buy_times, 8) -> 10
		;

			get(arena_extra_buy_times, 9) -> 10
		;

			get(arena_extra_buy_times, 10) -> 10
		;

			get(arena_extra_buy_times, 11) -> 10
		;

			get(arena_extra_buy_times, 12) -> 10
		;

			get(arena_extra_buy_times, 13) -> 15
		;

			get(arena_extra_buy_times, 14) -> 15
		;

			get(arena_extra_buy_times, 15) -> 15
		;

			get(arena_challenge_cd, 0) -> 300
		;

			get(arena_challenge_cd, 1) -> 300
		;

			get(arena_challenge_cd, 2) -> 150
		;

			get(arena_challenge_cd, 3) -> 0
		;

			get(arena_challenge_cd, 4) -> 0
		;

			get(arena_challenge_cd, 5) -> 0
		;

			get(arena_challenge_cd, 6) -> 0
		;

			get(arena_challenge_cd, 7) -> 0
		;

			get(arena_challenge_cd, 8) -> 0
		;

			get(arena_challenge_cd, 9) -> 0
		;

			get(arena_challenge_cd, 10) -> 0
		;

			get(arena_challenge_cd, 11) -> 0
		;

			get(arena_challenge_cd, 12) -> 0
		;

			get(arena_challenge_cd, 13) -> 0
		;

			get(arena_challenge_cd, 14) -> 0
		;

			get(arena_challenge_cd, 15) -> 0
		;

			get(max_donate_money_chief, 0) -> 3000000
		;

			get(max_donate_money_chief, 1) -> 3000000
		;

			get(max_donate_money_chief, 2) -> 3000000
		;

			get(max_donate_money_chief, 3) -> 3000000
		;

			get(max_donate_money_chief, 4) -> 3000000
		;

			get(max_donate_money_chief, 5) -> 4500000
		;

			get(max_donate_money_chief, 6) -> 4500000
		;

			get(max_donate_money_chief, 7) -> 4500000
		;

			get(max_donate_money_chief, 8) -> 4500000
		;

			get(max_donate_money_chief, 9) -> 4500000
		;

			get(max_donate_money_chief, 10) -> 4500000
		;

			get(max_donate_money_chief, 11) -> 4500000
		;

			get(max_donate_money_chief, 12) -> 6000000
		;

			get(max_donate_money_chief, 13) -> 6000000
		;

			get(max_donate_money_chief, 14) -> 6000000
		;

			get(max_donate_money_chief, 15) -> 6000000
		;

			get(max_donate_money_junshi, 0) -> 2400000
		;

			get(max_donate_money_junshi, 1) -> 2400000
		;

			get(max_donate_money_junshi, 2) -> 2400000
		;

			get(max_donate_money_junshi, 3) -> 2400000
		;

			get(max_donate_money_junshi, 4) -> 2400000
		;

			get(max_donate_money_junshi, 5) -> 3600000
		;

			get(max_donate_money_junshi, 6) -> 3600000
		;

			get(max_donate_money_junshi, 7) -> 3600000
		;

			get(max_donate_money_junshi, 8) -> 3600000
		;

			get(max_donate_money_junshi, 9) -> 3600000
		;

			get(max_donate_money_junshi, 10) -> 3600000
		;

			get(max_donate_money_junshi, 11) -> 3600000
		;

			get(max_donate_money_junshi, 12) -> 4800000
		;

			get(max_donate_money_junshi, 13) -> 4800000
		;

			get(max_donate_money_junshi, 14) -> 4800000
		;

			get(max_donate_money_junshi, 15) -> 4800000
		;

			get(max_donate_money_tangzhu, 0) -> 1800000
		;

			get(max_donate_money_tangzhu, 1) -> 1800000
		;

			get(max_donate_money_tangzhu, 2) -> 1800000
		;

			get(max_donate_money_tangzhu, 3) -> 1800000
		;

			get(max_donate_money_tangzhu, 4) -> 1800000
		;

			get(max_donate_money_tangzhu, 5) -> 2700000
		;

			get(max_donate_money_tangzhu, 6) -> 2700000
		;

			get(max_donate_money_tangzhu, 7) -> 2700000
		;

			get(max_donate_money_tangzhu, 8) -> 2700000
		;

			get(max_donate_money_tangzhu, 9) -> 2700000
		;

			get(max_donate_money_tangzhu, 10) -> 2700000
		;

			get(max_donate_money_tangzhu, 11) -> 2700000
		;

			get(max_donate_money_tangzhu, 12) -> 3600000
		;

			get(max_donate_money_tangzhu, 13) -> 3600000
		;

			get(max_donate_money_tangzhu, 14) -> 3600000
		;

			get(max_donate_money_tangzhu, 15) -> 3600000
		;

			get(max_donate_money_normal, 0) -> 1200000
		;

			get(max_donate_money_normal, 1) -> 1200000
		;

			get(max_donate_money_normal, 2) -> 1200000
		;

			get(max_donate_money_normal, 3) -> 1200000
		;

			get(max_donate_money_normal, 4) -> 1200000
		;

			get(max_donate_money_normal, 5) -> 1800000
		;

			get(max_donate_money_normal, 6) -> 1800000
		;

			get(max_donate_money_normal, 7) -> 1800000
		;

			get(max_donate_money_normal, 8) -> 1800000
		;

			get(max_donate_money_normal, 9) -> 1800000
		;

			get(max_donate_money_normal, 10) -> 1800000
		;

			get(max_donate_money_normal, 11) -> 1800000
		;

			get(max_donate_money_normal, 12) -> 2400000
		;

			get(max_donate_money_normal, 13) -> 2400000
		;

			get(max_donate_money_normal, 14) -> 2400000
		;

			get(max_donate_money_normal, 15) -> 2400000
		;

			get(upgrade_mail_title, 1) -> <<"白金贵族身份象征福利">>
		;

			get(upgrade_mail_title, 2) -> <<"钻石贵族身份象征福利">>
		;

			get(upgrade_mail_title, 3) -> <<"至尊贵族身份象征福利">>
		;

			get(upgrade_mail_title, 4) -> <<"王者贵族身份象征福利">>
		;

			get(upgrade_mail_title, 5) -> <<"帝尊贵族身份象征福利">>
		;

			get(upgrade_mail_title, 6) -> <<"天尊贵族身份象征福利">>
		;

			get(upgrade_mail_title, 7) -> 0
		;

			get(upgrade_mail_title, 8) -> 0
		;

			get(upgrade_mail_title, 9) -> 0
		;

			get(upgrade_mail_title, 10) -> 0
		;

			get(upgrade_mail_title, 11) -> 0
		;

			get(upgrade_mail_title, 12) -> 0
		;

			get(upgrade_mail_title, 13) -> 0
		;

			get(upgrade_mail_title, 14) -> 0
		;

			get(upgrade_mail_title, 15) -> 0
		;

			get(upgrade_mail_content, 1) -> <<"恭喜大侠成为白金贵族，为感谢你的支持，我们送上象征身份的白金贵族称号卡">>
		;

			get(upgrade_mail_content, 2) -> <<"恭喜大侠成为钻石贵族，为感谢你的支持，我们送上象征身份的钻石贵族称号卡">>
		;

			get(upgrade_mail_content, 3) -> <<"恭喜大侠成为至尊贵族，为感谢你的支持，我们送上象征身份的至尊贵族称号卡">>
		;

			get(upgrade_mail_content, 4) -> <<"恭喜大侠成为王者贵族，为感谢你的支持，我们送上象征身份的王者贵族称号卡">>
		;

			get(upgrade_mail_content, 5) -> <<"恭喜大侠成为帝尊贵族，为感谢你的支持，我们送上象征身份的帝尊贵族称号卡">>
		;

			get(upgrade_mail_content, 6) -> <<"恭喜大侠成为天尊贵族，为感谢你的支持，我们送上象征身份的天尊贵族称号卡">>
		;

			get(upgrade_mail_content, 7) -> 0
		;

			get(upgrade_mail_content, 8) -> 0
		;

			get(upgrade_mail_content, 9) -> 0
		;

			get(upgrade_mail_content, 10) -> 0
		;

			get(upgrade_mail_content, 11) -> 0
		;

			get(upgrade_mail_content, 12) -> 0
		;

			get(upgrade_mail_content, 13) -> 0
		;

			get(upgrade_mail_content, 14) -> 0
		;

			get(upgrade_mail_content, 15) -> 0
		;

			get(upgrade_mail_goods, 1) -> 0
		;

			get(upgrade_mail_goods, 2) -> 0
		;

			get(upgrade_mail_goods, 3) -> 0
		;

			get(upgrade_mail_goods, 4) -> 0
		;

			get(upgrade_mail_goods, 5) -> 0
		;

			get(upgrade_mail_goods, 6) -> 0
		;

			get(upgrade_mail_goods, 7) -> 0
		;

			get(upgrade_mail_goods, 8) -> 0
		;

			get(upgrade_mail_goods, 9) -> 0
		;

			get(upgrade_mail_goods, 10) -> 0
		;

			get(upgrade_mail_goods, 11) -> 0
		;

			get(upgrade_mail_goods, 12) -> 0
		;

			get(upgrade_mail_goods, 13) -> 0
		;

			get(upgrade_mail_goods, 14) -> 0
		;

			get(upgrade_mail_goods, 15) -> 0
		;

			get(daily_mail_title, 0) -> null
		;

			get(daily_mail_title, 1) -> <<"白金贵族福利">>
		;

			get(daily_mail_title, 2) -> <<"钻石贵族福利">>
		;

			get(daily_mail_title, 3) -> <<"至尊贵族福利">>
		;

			get(daily_mail_title, 4) -> <<"王者贵族福利">>
		;

			get(daily_mail_title, 5) -> <<"帝尊贵族福利">>
		;

			get(daily_mail_title, 6) -> <<"天尊贵族福利">>
		;

			get(daily_mail_title, 7) -> 0
		;

			get(daily_mail_title, 8) -> 0
		;

			get(daily_mail_title, 9) -> 0
		;

			get(daily_mail_title, 10) -> 0
		;

			get(daily_mail_title, 11) -> 0
		;

			get(daily_mail_title, 12) -> 0
		;

			get(daily_mail_title, 13) -> 0
		;

			get(daily_mail_title, 14) -> 0
		;

			get(daily_mail_title, 15) -> 0
		;

			get(daily_mail_content, 0) -> 0
		;

			get(daily_mail_content, 1) -> <<"大侠，白金贵族尊享白金礼包给你送达，祝你游戏愉快！">>
		;

			get(daily_mail_content, 2) -> <<"大侠，钻石贵族尊享钻石礼包给你送达，祝你游戏愉快！">>
		;

			get(daily_mail_content, 3) -> <<"大侠，至尊贵族尊享至尊礼包给你送达，祝你游戏愉快！">>
		;

			get(daily_mail_content, 4) -> <<"大侠，王者贵族尊享王者礼包给你送达，祝你游戏愉快！">>
		;

			get(daily_mail_content, 5) -> <<"大侠，帝尊贵族尊享帝尊礼包给你送达，祝你游戏愉快！">>
		;

			get(daily_mail_content, 6) -> <<"大侠，天尊贵族尊享天尊礼包给你送达，祝你游戏愉快！">>
		;

			get(daily_mail_content, 7) -> 0
		;

			get(daily_mail_content, 8) -> 0
		;

			get(daily_mail_content, 9) -> 0
		;

			get(daily_mail_content, 10) -> 0
		;

			get(daily_mail_content, 11) -> 0
		;

			get(daily_mail_content, 12) -> 0
		;

			get(daily_mail_content, 13) -> 0
		;

			get(daily_mail_content, 14) -> 0
		;

			get(daily_mail_content, 15) -> 0
		;

			get(daily_mail_goods, 0) -> 0
		;

			get(daily_mail_goods, 1) -> 0
		;

			get(daily_mail_goods, 2) -> 0
		;

			get(daily_mail_goods, 3) -> 0
		;

			get(daily_mail_goods, 4) -> 0
		;

			get(daily_mail_goods, 5) -> 0
		;

			get(daily_mail_goods, 6) -> 0
		;

			get(daily_mail_goods, 7) -> 0
		;

			get(daily_mail_goods, 8) -> 0
		;

			get(daily_mail_goods, 9) -> 0
		;

			get(daily_mail_goods, 10) -> 0
		;

			get(daily_mail_goods, 11) -> 0
		;

			get(daily_mail_goods, 12) -> 0
		;

			get(daily_mail_goods, 13) -> 0
		;

			get(daily_mail_goods, 14) -> 0
		;

			get(daily_mail_goods, 15) -> 0
		;

			get(use_multiple_exp_count, 0) -> 6
		;

			get(use_multiple_exp_count, 1) -> 6
		;

			get(use_multiple_exp_count, 2) -> 6
		;

			get(use_multiple_exp_count, 3) -> 7
		;

			get(use_multiple_exp_count, 4) -> 7
		;

			get(use_multiple_exp_count, 5) -> 7
		;

			get(use_multiple_exp_count, 6) -> 7
		;

			get(use_multiple_exp_count, 7) -> 8
		;

			get(use_multiple_exp_count, 8) -> 8
		;

			get(use_multiple_exp_count, 9) -> 8
		;

			get(use_multiple_exp_count, 10) -> 8
		;

			get(use_multiple_exp_count, 11) -> 9
		;

			get(use_multiple_exp_count, 12) -> 9
		;

			get(use_multiple_exp_count, 13) -> 9
		;

			get(use_multiple_exp_count, 14) -> 10
		;

			get(use_multiple_exp_count, 15) -> 10
		;

			get(climb_tower_quick_enter_layer, 0) -> 196
		;

			get(climb_tower_quick_enter_layer, 1) -> 196
		;

			get(climb_tower_quick_enter_layer, 2) -> 196
		;

			get(climb_tower_quick_enter_layer, 3) -> 196
		;

			get(climb_tower_quick_enter_layer, 4) -> 196
		;

			get(climb_tower_quick_enter_layer, 5) -> 196
		;

			get(climb_tower_quick_enter_layer, 6) -> 196
		;

			get(climb_tower_quick_enter_layer, 7) -> 196
		;

			get(climb_tower_quick_enter_layer, 8) -> 196
		;

			get(climb_tower_quick_enter_layer, 9) -> 196
		;

			get(climb_tower_quick_enter_layer, 10) -> 196
		;

			get(climb_tower_quick_enter_layer, 11) -> 196
		;

			get(climb_tower_quick_enter_layer, 12) -> 196
		;

			get(climb_tower_quick_enter_layer, 13) -> 196
		;

			get(climb_tower_quick_enter_layer, 14) -> 196
		;

			get(climb_tower_quick_enter_layer, 15) -> 196
		;

			get(max_hirer_times, 0) -> 20
		;

			get(max_hirer_times, 1) -> 20
		;

			get(max_hirer_times, 2) -> 20
		;

			get(max_hirer_times, 3) -> 20
		;

			get(max_hirer_times, 4) -> 20
		;

			get(max_hirer_times, 5) -> 20
		;

			get(max_hirer_times, 6) -> 20
		;

			get(max_hirer_times, 7) -> 25
		;

			get(max_hirer_times, 8) -> 25
		;

			get(max_hirer_times, 9) -> 30
		;

			get(max_hirer_times, 10) -> 30
		;

			get(max_hirer_times, 11) -> 35
		;

			get(max_hirer_times, 12) -> 35
		;

			get(max_hirer_times, 13) -> 40
		;

			get(max_hirer_times, 14) -> 45
		;

			get(max_hirer_times, 15) -> 50
		;

			get(cultivate_partner_success_raise, 0) -> 0
		;

			get(cultivate_partner_success_raise, 1) -> 0
		;

			get(cultivate_partner_success_raise, 2) -> 0
		;

			get(cultivate_partner_success_raise, 3) -> 0
		;

			get(cultivate_partner_success_raise, 4) -> 0
		;

			get(cultivate_partner_success_raise, 5) -> 0
		;

			get(cultivate_partner_success_raise, 6) -> 0.05
		;

			get(cultivate_partner_success_raise, 7) -> 0.05
		;

			get(cultivate_partner_success_raise, 8) -> 0.05
		;

			get(cultivate_partner_success_raise, 9) -> 0.05
		;

			get(cultivate_partner_success_raise, 10) -> 0.05
		;

			get(cultivate_partner_success_raise, 11) -> 0.05
		;

			get(cultivate_partner_success_raise, 12) -> 0.05
		;

			get(cultivate_partner_success_raise, 13) -> 0.05
		;

			get(cultivate_partner_success_raise, 14) -> 0.05
		;

			get(cultivate_partner_success_raise, 15) -> 0.05
		;

			get(drop_exp_addition, 0) -> 0
		;

			get(drop_exp_addition, 1) -> 0
		;

			get(drop_exp_addition, 2) -> 0
		;

			get(drop_exp_addition, 3) -> 0
		;

			get(drop_exp_addition, 4) -> 0
		;

			get(drop_exp_addition, 5) -> 0
		;

			get(drop_exp_addition, 6) -> 0
		;

			get(drop_exp_addition, 7) -> 0
		;

			get(drop_exp_addition, 8) -> 0.05
		;

			get(drop_exp_addition, 9) -> 0.05
		;

			get(drop_exp_addition, 10) -> 0.05
		;

			get(drop_exp_addition, 11) -> 0.05
		;

			get(drop_exp_addition, 12) -> 0.05
		;

			get(drop_exp_addition, 13) -> 0.05
		;

			get(drop_exp_addition, 14) -> 0.05
		;

			get(drop_exp_addition, 15) -> 0.05
		;

			get(vip_ballon, 0) -> 0
		;

			get(vip_ballon, 1) -> 0
		;

			get(vip_ballon, 2) -> 0
		;

			get(vip_ballon, 3) -> 0
		;

			get(vip_ballon, 4) -> 0
		;

			get(vip_ballon, 5) -> 0
		;

			get(vip_ballon, 6) -> 0
		;

			get(vip_ballon, 7) -> 0
		;

			get(vip_ballon, 8) -> 0
		;

			get(vip_ballon, 9) -> 0
		;

			get(vip_ballon, 10) -> 1
		;

			get(vip_ballon, 11) -> 1
		;

			get(vip_ballon, 12) -> 1
		;

			get(vip_ballon, 13) -> 1
		;

			get(vip_ballon, 14) -> 1
		;

			get(vip_ballon, 15) -> 2
		;

			get(transport_free_times, 0) -> 1
		;

			get(transport_free_times, 1) -> 1
		;

			get(transport_free_times, 2) -> 1
		;

			get(transport_free_times, 3) -> 1
		;

			get(transport_free_times, 4) -> 1
		;

			get(transport_free_times, 5) -> 1
		;

			get(transport_free_times, 6) -> 1
		;

			get(transport_free_times, 7) -> 1
		;

			get(transport_free_times, 8) -> 1
		;

			get(transport_free_times, 9) -> 1
		;

			get(transport_free_times, 10) -> 1
		;

			get(transport_free_times, 11) -> 1
		;

			get(transport_free_times, 12) -> 1
		;

			get(transport_free_times, 13) -> 1
		;

			get(transport_free_times, 14) -> 1
		;

			get(transport_free_times, 15) -> 1
		;

			get(tve_extra_reward_time, 0) -> 0
		;

			get(tve_extra_reward_time, 1) -> 0
		;

			get(tve_extra_reward_time, 2) -> 1
		;

			get(tve_extra_reward_time, 3) -> 1
		;

			get(tve_extra_reward_time, 4) -> 1
		;

			get(tve_extra_reward_time, 5) -> 1
		;

			get(tve_extra_reward_time, 6) -> 1
		;

			get(tve_extra_reward_time, 7) -> 1
		;

			get(tve_extra_reward_time, 8) -> 1
		;

			get(tve_extra_reward_time, 9) -> 1
		;

			get(tve_extra_reward_time, 10) -> 1
		;

			get(tve_extra_reward_time, 11) -> 1
		;

			get(tve_extra_reward_time, 12) -> 1
		;

			get(tve_extra_reward_time, 13) -> 1
		;

			get(tve_extra_reward_time, 14) -> 1
		;

			get(tve_extra_reward_time, 15) -> 1
		;

			get(use_catch_ghost_multiple_exp_count, 0) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 1) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 2) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 3) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 4) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 5) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 6) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 7) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 8) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 9) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 10) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 11) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 12) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 13) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 14) -> 3
		;

			get(use_catch_ghost_multiple_exp_count, 15) -> 3
		;

			get(daily_catch_ghost_time, 0) -> 0
		;

			get(daily_catch_ghost_time, 1) -> 0
		;

			get(daily_catch_ghost_time, 2) -> 0
		;

			get(daily_catch_ghost_time, 3) -> 0
		;

			get(daily_catch_ghost_time, 4) -> 2
		;

			get(daily_catch_ghost_time, 5) -> 10
		;

			get(daily_catch_ghost_time, 6) -> 10
		;

			get(daily_catch_ghost_time, 7) -> 0
		;

			get(daily_catch_ghost_time, 8) -> 0
		;

			get(daily_catch_ghost_time, 9) -> 0
		;

			get(daily_catch_ghost_time, 10) -> 0
		;

			get(daily_catch_ghost_time, 11) -> 0
		;

			get(daily_catch_ghost_time, 12) -> 0
		;

			get(daily_catch_ghost_time, 13) -> 0
		;

			get(daily_catch_ghost_time, 14) -> 0
		;

			get(daily_catch_ghost_time, 15) -> 0
		;

			get(battle_speed_up, 0) -> 0
		;

			get(battle_speed_up, 1) -> 0
		;

			get(battle_speed_up, 2) -> 0
		;

			get(battle_speed_up, 3) -> 200
		;

			get(battle_speed_up, 4) -> 200
		;

			get(battle_speed_up, 5) -> 300
		;

			get(battle_speed_up, 6) -> 400
		;

			get(battle_speed_up, 7) -> 0
		;

			get(battle_speed_up, 8) -> 0
		;

			get(battle_speed_up, 9) -> 0
		;

			get(battle_speed_up, 10) -> 0
		;

			get(battle_speed_up, 11) -> 0
		;

			get(battle_speed_up, 12) -> 0
		;

			get(battle_speed_up, 13) -> 0
		;

			get(battle_speed_up, 14) -> 0
		;

			get(battle_speed_up, 15) -> 0
		;

			get(move_speed_up, 0) -> 0
		;

			get(move_speed_up, 1) -> 0
		;

			get(move_speed_up, 2) -> 0
		;

			get(move_speed_up, 3) -> 150
		;

			get(move_speed_up, 4) -> 150
		;

			get(move_speed_up, 5) -> 200
		;

			get(move_speed_up, 6) -> 300
		;

			get(move_speed_up, 7) -> 0
		;

			get(move_speed_up, 8) -> 0
		;

			get(move_speed_up, 9) -> 0
		;

			get(move_speed_up, 10) -> 0
		;

			get(move_speed_up, 11) -> 0
		;

			get(move_speed_up, 12) -> 0
		;

			get(move_speed_up, 13) -> 0
		;

			get(move_speed_up, 14) -> 0
		;

			get(move_speed_up, 15) -> 0
		;

			get(sanjieyishi_extra_time, 0) -> 0
		;

			get(sanjieyishi_extra_time, 1) -> 0
		;

			get(sanjieyishi_extra_time, 2) -> 0
		;

			get(sanjieyishi_extra_time, 3) -> 0
		;

			get(sanjieyishi_extra_time, 4) -> 0
		;

			get(sanjieyishi_extra_time, 5) -> 1
		;

			get(sanjieyishi_extra_time, 6) -> 1
		;

			get(sanjieyishi_extra_time, 7) -> 0
		;

			get(sanjieyishi_extra_time, 8) -> 0
		;

			get(sanjieyishi_extra_time, 9) -> 0
		;

			get(sanjieyishi_extra_time, 10) -> 0
		;

			get(sanjieyishi_extra_time, 11) -> 0
		;

			get(sanjieyishi_extra_time, 12) -> 0
		;

			get(sanjieyishi_extra_time, 13) -> 0
		;

			get(sanjieyishi_extra_time, 14) -> 0
		;

			get(sanjieyishi_extra_time, 15) -> 0
		;

		get(_Type, _Lv) ->
			?WARNING_MSG("Invalid vip welfare ~p ~p", [_Type, _Lv]),
        	null.

		
